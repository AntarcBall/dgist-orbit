# DGIST Orbit Dev Log

- 2026-04-20 23:25 KST: Added scan-shadow sectors behind unstable first-hop DGIST corridors so coverage now has real blind spots instead of pretending every in-range building is equally scannable. Before, a hot adjacent building like `전기전자컴퓨터공학관 (E3)` could flood the local sweep with noise, but downstream sectors still showed up as normal remote targets even when that corridor should have occluded them. After, downstream sectors hidden behind a collapsing first-hop corridor drop out of the actual scan target pool, the scan CTA/HUD call out `음영 N`, coverage badges can flip to an orange `음영` state with the blocking neighbor named on the badge, and zone popups plus the current focus HUD explain which DGIST building is casting the scan shadow. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then using a same-origin headless Chrome helper page to seed `화학물리학관 (E2)` with a broken `전기전자컴퓨터공학관 (E3)` corridor, confirming the dumped DOM rendered `shadow_badges=1`, `음영 1`, and repeated `전기전자컴퓨터공학관 (E3) 뒤` shadow copy, with no obvious runtime failure beyond Chrome's usual shared-memory stderr noise in headless mode.

- 2026-04-20 22:18 KST: Added sector-origin threat spill previews so hotspot sectors now reveal who they are actively pressuring, instead of only rendering as isolated red/orange danger nodes. Before, a hot building could glow on the map and raise route pressure, but neither the focus HUD nor the stability list explained which neighboring DGIST corridor was about to absorb that pressure next. After, focused sectors get a new `위협 번짐` row with outgoing pressure chips, stability cards surface compact `압박 N` pills and swap their note copy to the same neighbor-pressure summary, zone popups echo the spill targets, and the map itself draws short spill vectors plus `압박 유입` beacons onto the threatened neighbors. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading a same-origin headless Chrome validation harness with forced hot states on `화학물리학관 (E2)` and `컨실리언스홀 (E7)`, confirming the focus HUD rendered `압박 3축`, the `화학물리학관 (E2)` stability card switched to `압박 3`, the popup listed `위협 번짐: 전기전자컴퓨터공학관 (E3) 직격 압박 · 대학본부 (E1) 직격 압박`, 8 `.threat-spill-beacon-core` markers rendered, and no runtime errors were captured.

- 2026-04-20 21:20 KST: Surfaced local scan interference sources directly on the live campus map so scan noise now reads as spatial pressure instead of a bare number. Before, the scan legend could say `간섭 2`, but there was no visual explanation for which neighboring DGIST sector was polluting the current sweep. After, hostile adjacent sectors draw orange interference vectors into the current origin, render compact `스캔 노이즈 +N` beacons at the source, and the local scan legend plus current-zone focus copy both name the same jammer. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading a same-origin validation harness in headless Chrome with a forced `대학본부 (E1)` save where `화학물리학관 (E2)` had threat 3, confirming the page rendered 1 `.scan-interference-beacon-core` with `스캔 노이즈 +2`, and confirming the live legend and focus HUD both switched to `간섭 2` plus `노이즈 화학물리학관 (E2)` without runtime failure.

- 2026-04-20 20:23 KST: Added live scan coverage badges directly onto covered DGIST sectors so scan reach now reads on the campus map itself instead of hiding entirely in the focus HUD. Before, you had to infer whether a nearby sector was the current `1링` scan target from the radar card and CTA legend, even though the map was already the visual center. After, covered zones render compact floating badges like `1링`, `정조준`, or blocked relay state right above their map node, and tapping a badge focuses that sector immediately. Verified by serving the static build at `http://127.0.0.1:4173`, loading it in headless Chromium via Playwright from the shell, confirming the page rendered 1 `.coverage-badge` on a fresh save with no captured console/runtime errors, clicking that badge, and confirming the focus HUD title switched from `대학본부 (E1)` to `화학물리학관 (E2)` before capturing a validation screenshot.

- 2026-04-20 19:24 KST: Let the stability list drive the live map preview instead of leaving frontier planning trapped in the radar HUD. Before, stability cards told you how many `신규 스캔` sectors a move would unlock, but on mobile you still had to leave that list and reselect the same sector in the map focus radar to see the actual campus overlay. After, every stability card has a `프론티어 보기` action that focuses the matching sector, opens its popup, and projects the same redeploy frontier anchor, gain traces, and scan badges directly onto the map before travel. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then launching fresh headless Chrome on a clean debug port, tapping `프론티어 보기` for `화학물리학관 (E2)`, and confirming the focus HUD title switched to `화학물리학관 (E2)`, the popup matched, 1 `.redeploy-preview-anchor` rendered, 4 gain badges appeared, and no runtime exceptions were captured.

- 2026-04-20 18:24 KST: Projected redeploy frontier previews onto the live campus map so scan-range gains read as spatial overlays instead of HUD text alone. Before, `재배치 후 스캔 프론티어` only listed gained/lost sectors in the focus HUD and supporting panels, so the player still had to mentally translate that forecast back onto the DGIST layer. After, focusing a redeploy target draws a cyan anchor on the destination, paints dashed gain traces from that target toward newly reachable sectors, and tags those sectors with compact `신규` badges while any dropped axes get muted `이탈` traces. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then launching fresh headless Chrome with service workers disabled on a clean debug port, selecting `화학물리학관 (E2)` through the real focus radar, and confirming the page rendered `재배치 후 스캔 프론티어`, 1 `.redeploy-preview-anchor`, 4 `신규` preview badges, and live gain-path SVG overlays with no captured runtime errors.

- 2026-04-20 17:15 KST: Added redeploy scan-frontier previews so movement decisions expose actual spatial scan gains before the player commits. Before, moving to a new DGIST sector required mentally inferring whether the new position would open better coverage or just relocate the same scan radius. After, the focus HUD has a dedicated `재배치 후 스캔 프론티어` row with new-sector chips and dropped-line chips, stability cards show compact `신규 스캔` counts for each destination, and zone popups echo the same forecast in plain language. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then driving headless Chrome over CDP to load the real page, click the `창업지원 센터` marker, confirm the focus HUD switched to `신규 3개 섹터 포착` with frontier chips for `종합체육관`, `디지스트 축구장`, and `정보통신융합공학관 (E3)`, and confirm the popup plus stability card both reflected the same frontier forecast with no captured runtime errors.

- 2026-04-20 16:32 KST: Formalized the next real-campus satellite milestone instead of treating the map as a loose backdrop. Added `docs/campus-alignment-spec.md` to define the execution contract for small-angle satellite rotation correction, rectangle-first building segmentation, and label transfer from the white DGIST map. Added `data/campus_reference_labels.json` with seed anchors for major DGIST buildings and landmarks inferred from the provided reference map, plus `scripts/campus_align.py`, a runnable OpenCV first pass that estimates rotation in a constrained `-15°..15°` window, segments building-like rectangles, and exports overlay/review artifacts. This pass is deliberately reviewable rather than overclaiming certainty, so the generated segment-to-label mapping remains a suggestion until the overlays are visually checked.

- 2026-04-20 16:15 KST: Added first-hop relay gating to long-range targeted scans, so remote coverage now depends on whether the local DGIST relay sector can actually carry the packet. Before, any covered 2-hop zone could still be treated as a viable target even if the first corridor out of the player’s current sector was already collapsing, which made some remote aiming feel more cosmetic than spatial. After, unstable first-hop relays can hard-block that direction, the scan CTA flips to a blocked state with a `근접 스캔` fallback, the focus HUD explains which relay sector is failing, the map paints the remote corridor in red and pins a `원격 차단` beacon on the broken relay, and zone popups also note the blocked line. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then driving headless Chrome over CDP with a forced save state centered on `학술정보관 (E8)` and a broken `창업지원 센터` relay, confirming the UI switched to `근접 스캔` / `차단 스캔`, rendered the blocked HUD note plus `.scan-blocked-beacon-core`, and captured no runtime exceptions beyond the expected PWA install-banner info log.

- 2026-04-20 15:15 KST: Surfaced relay-slip risk directly on the campus map trace so unstable remote scans now show where the packet will get caught before you tap. Before, `중계 이탈` only appeared as text in the scan legend and focus HUD, so the player still had to mentally translate that warning back onto the map. After, slip-prone targeted scans tint the trace hotter, paint the actual unstable corridor segment in red, pin a `중계 이탈` beacon on the intercepting DGIST zone, and leave a faint ghost line toward the original destination so the miss path reads spatially at a glance. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then driving headless Chrome over CDP with a forced unstable save state to focus `디지스트 축구장 → 정보통신융합공학관 (E3)`, confirm the legend switched to `중계 이탈 · 학술정보관 (E8)`, and confirm `.scan-slip-segment`, `.scan-slip-beacon-core`, and `.scan-slip-ghost-line` rendered with no captured runtime errors.

- 2026-04-20 13:15 KST: Made remote scan quality depend on actual campus corridor state instead of treating every targeted scan as equally reliable once it was in range. Before, selecting a remote zone changed the target and route-pressure text, but a scan fired through a secure relay and a scan fired through a shaky corridor still behaved almost the same. After, targeted scans now compute relay integrity from the real route corridor mix (`확보`, `경계`, `교란`), surface that as `릴레이 선명/유지/흔들림/불안정` in both the scan CTA legend and the focus HUD, and use it to modulate remote scan reliability, XP, loot chance, and log copy so unstable corridors visibly degrade intel. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then driving headless Chrome over CDP to load the real game, select a remote orbit node from the focus radar, confirm the UI switched to `정조준 스캔` with relay chips like `릴레이 유지`, click scan, and confirm the resulting intel/logs included the new relay-quality text with no captured runtime errors.

- 2026-04-20 12:15 KST: Replaced the focus HUD's plain connected-sector row with a radar-anchored sector selector around the main zone core, so scan retargeting now feels like operating a local orbit network instead of reading a chip list. Before, `접속 섹터` was just a flat stack of named pills under the HUD, which made the satellite layer mechanically useful but visually list-like. After, the selected/current zone sits at the center of the radar, neighboring sectors orbit it as touchable nodes, and focused or targeted sectors get their own highlight states while the supporting chip row drops into a quieter secondary role. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading a same-origin headless Chrome harness that opened the real game, confirmed `.zone-focus-radar-cluster` and `.zone-focus-orbit-node` rendered, and validated that the focus HUD still showed a targeted scan state (`정조준 스캔` plus trace note) with the map loaded and no obvious runtime failure.

- 2026-04-20 11:15 KST: Added a compact scan target legend beside the main `위성 스캔` CTA so the map header now tells you what the next scan is actually aimed at before tapping. Before, the header only showed the button itself, so you had to infer scan mode from the larger focus HUD or click first to see whether the system would do a local sweep or a targeted remote scan. After, the CTA cluster shows `광역 스캔` versus `정조준 스캔`, the active zone name, coverage/hop info, reliability, and either local interference or remote route pressure in one tight card that stays readable on mobile. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading the page in headless Chrome, confirming the new legend rendered in the dumped DOM as `광역 스캔 / DGIST 정문 링크 / 근접 1링`, and using a CDP automation script to click map overlays until the legend switched to targeted states like `정조준 스캔 / 학술정보관 코어 / 직접 1홉 / 압박 2` without obvious runtime failure.

- 2026-04-20 10:15 KST: Added compact equip comparison strips to gear cards in the `장비 및 인벤토리` dock so slot replacement decisions read instantly on mobile. Before, the new loadout HUD showed what was equipped, but each gear card below it was still mostly description text plus a generic `장착` button, so you had to mentally compare items against the current slot. After, every gear card now shows whether its slot is empty, already equipped, or replacing a named item, plus pill deltas like `공격 +2`, `치명 +10%`, or red downgrade chips when a swap would reduce stats. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading a temporary same-origin browser harness in headless Chrome that switched to the inventory tab and confirmed comparison blocks rendered on gear cards with active slot notes and stat chips, without obvious runtime failure. Playwright was unavailable from the shell on this pass, so headless Chrome DOM automation was the strongest fallback.

- 2026-04-20 09:15 KST: Added a real loadout HUD inside the `현장 도크` inventory view so equipped gear reads at a glance instead of hiding in the item list. Before, the dock only showed a short `장착 중:` text line plus raw inventory cards, so the equipment pane still felt like a text stack. After, the inventory tab opens with a four-slot equipment cluster (`툴`, `보조`, `암`, `바디`) and compact derived-stat chips for 공격, 방어, 테크, 스캔, which makes loadout state readable in one mobile-friendly glance. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading the page in fresh headless Chrome over the DevTools Protocol, switching to `장비 및 인벤토리`, and confirming the new `.loadout-hud` rendered with four slot cards, four stat chips, and the inventory list without obvious runtime failure.

- 2026-04-20 07:15 KST: Turned focused remote zones into real targeted scans with a directional cone/trace on the campus map. Before, clicking a zone only swapped the focus card while `위성 스캔` still behaved like a generic rotating remote pick, so the satellite layer looked radial but not aimed. After, selecting a covered neighbor updates the CTA to `정조준 스캔`, draws a gold trace wedge from the current sector toward that zone, and makes the next scan resolve against that selected direction with matching focus-card copy. Verified by serving the static build at `http://127.0.0.1:4173` and running a real headless Playwright Chromium check from a temp Node runner that clicked map markers until `학술정보관 코어` became focused, confirmed the `정조준 스캔` button plus `.scan-trace-note`/targeted overlay path appeared, clicked scan, and confirmed fresh directional intel with no captured console or runtime errors.

- 2026-04-20 06:18 KST: Projected the active satellite coverage directly onto the campus map with pulsing scan-radius rings and live scan/control links from the current sector. Before, scan range and control support mostly lived in the focus HUD text, so the satellite layer still felt partially informational instead of spatial. After, the player’s current node visibly broadcasts its reachable rings, adjacent scan links, and secured local links on the map itself, making scan position feel more like a tactical emitter. Verified by serving the static build locally, running a real headless browser smoke check with Playwright-driven Chrome from a temp runner, confirming the page loaded at `http://127.0.0.1:4173`, the new `.scan-radius-ring` overlay rendered, clicking `위성 스캔`, and confirming new intel appeared with no captured console or runtime errors.

- 2026-04-20 05:20 KST: Drew sector control directly onto the campus map so links now read as `확보 회랑`, `경계 회랑`, or `교란 회랑`, and controlled nodes get a brighter cyan ring on the playfield. Before, the new control-network rules mostly lived in the focus/stability UI, so you still had to read panels to infer which routes were safe. After, the map itself exposes safer corridors and stressed routes at a glance, which makes the satellite layer feel more like tactical terrain instead of wallpaper. Verified by serving the static build locally, running `node --check` on `game.js` and `data.js`, then loading `http://127.0.0.1:4173` in headless Chrome with DOM dump and screenshot capture to confirm the new corridor legend and colored route overlays rendered without obvious runtime failure. Playwright package access was unavailable from the shell on this pass, so headless Chrome was the strongest automation fallback.

- 2026-04-20 04:19 KST: Merged the separate `위성 인텔` and `DGIST 도감` cards into one tabbed `작전 콘솔` panel so the sidebar stops stacking as many always-open cards beside the map. Before, intel and dossier each consumed their own full card, which made the lower-right UI feel more like a dashboard column than a focused field interface. After, those views share one compact panel with segmented tabs, reducing visible panel count while keeping both information surfaces one tap away on mobile. Verified by serving the static build locally, loading `http://127.0.0.1:4173` in headless Chrome over CDP, confirming the page rendered with the new `작전 콘솔`, then clicking from `위성 인텔` to `DGIST 도감` and confirming the active tab, panel visibility, and dossier content updated with no captured runtime or console errors.

- 2026-04-20 03:20 KST: Slimmed the map focus overlay into a tighter tactical HUD so the map keeps visual priority instead of fighting a bulky nested card. Before, the focus overlay stacked a large radar, full story block, and multiple dashboard-like sections that made the map corner feel heavy. After, the same information is compressed into a smaller 388px overlay with a top-line radar, compact metric tiles, and three clear signal/control/route rows with chip-style readouts. Verified by serving the static build locally, loading `http://127.0.0.1:4173` in headless Chromium through Playwright, clicking `위성 스캔`, and confirming the page rendered with a 3-row HUD (`hudWidth: 388`) and no console or runtime errors.

- 2026-04-20 01:42 KST: Recorded the current visual problems after the first map-priority rebalance so future redesign work stays anchored to the right targets. The current judged issues are: the screen still reads too much like `dashboard + map`, the map focus overlay is too bulky and nested, the top hero/header block is still visually heavy, and the satellite/orbit fantasy is still weaker than the game concept demands. These points were written into `DESIGN_NOTES.md` and converted into queued tasks in `WORK_QUEUE.md` so the next UI pass can optimize against a stable critique instead of drifting back into more generic panel reshuffling.

- 2026-04-20 00:15 KST: Added a campus-sector control layer so local stabilization now has spatial consequences instead of being a purely local number bump. Before, stabilizing a zone only affected that zone, and scans cared mostly about your current node plus generic adjacency/interference. After, a zone can become a control anchor when it is stable and low-threat, nearby safe sectors feed a visible `캠퍼스 제어망` block in the focus overlay, stability cards show whether each sector is part of the network, stabilization sends a small support wave into adjacent sectors, and controlled positions reduce scan interference / surge propagation while sometimes extending scan reach. Verified by serving the static build locally, running `node --check` on `game.js` and `data.js`, then loading the page in headless Chrome over the DevTools Protocol, clearing save data, confirming the initial focus card showed `제어권 미확보`, clicking 휴식 + 안정화 at DGIST 정문 링크 to flip the UI to `제어 앵커 확보`, and running a scan to confirm the runtime log reported the new control-network surge reduction with no obvious runtime failure.
- 2026-04-19 23:18 KST: Added a route strip to the map focus card so selected zones now show the shortest redeploy path across campus as named stops instead of only a hop count. Before, long moves like 정문 링크 → 학술정보관 코어 → 연구동 코리도어 → AI·로봇 연구 코어 were summarized as abstract text. After, the focus overlay renders the path as an intentional chip-and-arrow cluster with the current zone and target visually marked, which makes cross-campus travel feel navigable. Verified by serving the static build locally, running `node --check` on `game.js` and `data.js`, then loading the page in headless Chrome and confirming the new `재배치 경로` route strip rendered in the focus card with no obvious runtime failure. Playwright was unavailable in this environment, so I used headless Chrome DOM inspection as the strongest browser automation fallback.
- 2026-04-19 22:28 KST: Re-anchored the campus layer around a more DGIST-like route spine instead of the old nearest-point scatter. Zone names and coordinates now read like a central academic, plaza, dorm, lake, and east-research traversal, the map draws explicit route links, and movement cost now scales with campus hop distance so redeploying to AI·로봇 연구 코어 from the main gate feels like a real cross-campus move. Before, travel cost ignored route distance and the sector graph was auto-generated from arbitrary proximity. After, the focus card and stability panel both show hop-aware movement. Verified by serving the static build locally, running `node --check` on `game.js` and `data.js`, then using headless Chrome over CDP to reload a fresh save, confirm the new route labels and sector links rendered, travel from DGIST 정문 링크 to AI·로봇 연구 코어, and confirm the UI/logs updated with no runtime errors.
- 2026-04-19 15:15 KST: Added boss-only signature cards and phase shifts for Innovation Plaza and AI Lab Core. Boss encounters now show a named mechanic before combat, then switch to a second phase with a stronger signature state at 50% HP. Verified by running a local server and a headless Playwright smoke test in Chrome that seeded Innovation Plaza, triggered a boss via scan, entered battle, and confirmed both the phase-one and phase-two UI states appeared without runtime failure.
- 2026-04-19 16:17 KST: Added a basic PWA app shell with manifest, orbit icon, install CTA, and service worker caching for the static build. The hero now surfaces app-shell readiness and can expose a home-screen install prompt on supported browsers. Verified by serving the game locally, fetching manifest.webmanifest and sw.js over HTTP, and loading the page in headless Chrome to confirm the rendered install-status text updated to the offline-ready state without obvious runtime errors.
- 2026-04-19 17:20 KST: Added quick-travel controls to the campus stability panel so mobile players can move between zones without precise map taps. Each zone card now shows live travel energy cost, highlights the active zone, and disables its own move button when already deployed there. Verified by serving the game locally and driving headless Chrome over the DevTools Protocol to clear save data, click the library quick-travel button, and confirm the hero switched to 정보 코어 라이브러리 with the current-zone control disabled. Playwright was unavailable in this environment, so I used CDP as the strongest browser automation fallback.
- 2026-04-19 18:19 KST: Added a combat intent preview card that surfaces the enemy's next move severity and projected incoming damage after the current shield value, so fights read more clearly on first glance. Verified by serving the game locally, forcing a fresh reload in headless Chrome via CDP, entering an Innovation Plaza boss fight, and confirming the new intent panel rendered with a severity badge and projected damage text without runtime failure.
- 2026-04-19 19:18 KST: Replaced the flat encounter/combat button stack with an orbit-style action cluster centered on a primary core button, so the panel reads like a tactical console instead of a text list. Field state now anchors 교전 시작 in the middle with stabilization below, while battle wraps 분석, 보호막, 버스트, 재밍, and 배터리 around the core with live readiness text. Verified by serving the game locally and using headless Chrome over CDP to confirm the idle field orbit rendered, then travel to 이노베이션 플라자, force a scan encounter, enter battle, and verify the battle orbit showed all six action nodes without runtime failure.
- 2026-04-19 20:24 KST: Added a map-anchored zone focus overlay so the current or selected campus area now reads like a tactical target card instead of leaving the map visually unframed. The map now carries a sci-fi focus panel with a radar core, live stability/threat/difficulty metrics, and a direct travel CTA that locks into 현재 배치 once deployed. Verified by serving the game locally and using headless Chrome over CDP to load the page, confirm the new 정문 링크 focus card rendered, click-travel to 정보 코어 라이브러리, and confirm both the hero header and focus overlay updated without runtime failure.
- 2026-04-19 21:22 KST: Made satellite scanning positional instead of global flavor text. The zone focus card now shows connected sectors plus live scan radius/interference, and scans from the current zone can now read adjacent sectors, extend farther from stable research/knowledge hubs, and raise threat remotely when neighboring signals flare. Before, the scan button always produced essentially location-agnostic feed lines. After, the top intel line names the scanned direction and target sector, and map popups/focus UI expose the local campus graph. Verified by running the static build on the existing local server, syntax-checking `game.js` with `node --check`, then using headless Chrome over CDP to reload the page, confirm the new connected-sector cluster rendered, click 위성 스캔, and verify the latest intel changed to a directional sector-specific scan result with no captured runtime errors.
- 2026-04-20 02:17 KST: Compressed the landing-style hero into a tactical status bar so the game opens with a lighter operations strip instead of a marketing block. Before, the header used a large title/copy section that made the page read like a dashboard landing screen above the actual map. After, the top bar presents the product ID, ops label, install state, and the three live status pills in one compact row, which lets the map take visual priority immediately. Verified by serving the static build locally, running `node --check` on `game.js` and `data.js`, then loading the page in headless Chrome, confirming the compact header rendered with `CAMPUS SIGNAL OPS` plus the live status pills, and capturing a screenshot with no obvious runtime failure.
- 2026-04-20 08:15 KST: Folded the separate quest and inventory cards into one tabbed `현장 도크` so the lower sidebar loses a standing panel and mobile play can swap between objectives and gear without scrolling through two stacked cards. Before, `작전 목록` and `장비 및 인벤토리` occupied separate cards, which kept the sidebar looking heavier than the now map-first layout. After, both live inside one segmented dock with a quick reroll action, quest view, and equipment view that keeps the equipped-loadout note attached to the inventory pane. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then loading the page in headless Chrome over CDP, confirming the new `#logistics-tabs` rendered, switching from quests to inventory and back, checking that 3 quest items and 4 inventory items appeared with the loadout text, and capturing a screenshot with no obvious runtime failure.

- 2026-04-20 14:24 KST: Added relay-slip resolution to targeted remote scans so unstable campus corridors now intercept a scan before it reaches the intended far sector. Before, even a badly degraded route still let `정조준 스캔` land on the chosen remote zone, which made weak relay lines feel like flavor more than gameplay. After, very unstable targeted routes surface a `중계 이탈` warning in the scan legend, the focus HUD explicitly names the first shaky relay sector that may catch packets, and the actual scan can resolve onto that intermediate DGIST corridor instead of the original far target, with matching intel/log copy. Verified by serving the static build at `http://127.0.0.1:4173`, running `node --check` on `game.js` and `data.js`, then driving a fresh headless Chrome CDP session against a forced unstable save state to click `AI·로봇 연구 코어`, confirm the live legend showed `릴레이 불안정` plus `중계 이탈 · 연구동 코리도어`, run the scan, and confirm the resulting intel/logs landed on `연구동 코리도어` with no captured runtime errors.
